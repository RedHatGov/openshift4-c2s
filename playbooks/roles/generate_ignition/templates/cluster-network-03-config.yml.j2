apiVersion: operator.openshift.io/v1
kind: Network
metadata:
  name: cluster
spec: 
  defaultNetwork:
    openshiftSDNConfig:
      mode: {{ network_policy }}
      mtu: {{ mtu }}
      vxlanPort: {{ vxlanPort }}
